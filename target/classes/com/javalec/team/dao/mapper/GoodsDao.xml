<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.javalec.team.dao.GoodsDao">

     <select id="getGoods" resultType="com.javalec.team.dto.GoodsDto" parameterType="hashmap">
        <!-- select mem_pwd from mvc_member where mem_uid=#{mem_uid} --> 
        select g_name,g_code,g_price from goods where g_code = #{g_code}
    </select> 
 
   <insert id="insertBuy">
		insert into history(his_code,his_amount,u_id,g_code,g_price) VALUES(HISTORY_SEQ.nextval,${c_amount},#{u_id},${g_code},${g_price})
    </insert>
 
       

       
</mapper>